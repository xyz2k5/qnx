#include <stdio.h>
#include <unistd.h>
#include <sys/wait.h>

int main()
{
    pid_t p;

    p = fork();

    if (p == 0)
    {
        // Child process
        printf("This is CHILD process\n");
        printf("Child PID = %d\n", getpid());
    }
    else
    {
        // Parent process
    	wait (NULL);
        printf("This is PARENT process\n");
        printf("Parent PID = %d\n", getpid());
    }

    return 0;
}
